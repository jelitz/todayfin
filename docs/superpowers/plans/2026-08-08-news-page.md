# news-page (뉴스 페이지 신설) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `#/news` 뉴스 페이지 신설 — 상단 헤드라인 5건 고정 블록 + 주요뉴스/최신뉴스 탭 피드(토스증권 벤치마크, todayfin 디자인 철학).

**Architecture:** 스냅숏 + 클라이언트 정렬(A안). 파이프라인은 수집 상한 5→70 상수 변경뿐이고, `data/news.json` 스키마 불변(배열 순서 = 구글 랭킹). 프런트는 News(컨테이너, fetch·3-상태 분기) / NewsView(프레젠테이션, props 주입) 분리 — repo의 jsdom 없는 정적 렌더 테스트 전략과 정합.

**Tech Stack:** Vite + React 19 + TypeScript, vitest(renderToStaticMarkup), Python(pytest), 해시 라우팅.

**Spec:** `docs/specs/news-page/`(requirements·design·tasks — 3-agent 적대적 검증 반영판, 사용자 승인 완료). 이 계획의 Task N은 스펙 tasks.md의 T N과 1:1.

## Global Constraints

- 디자인: 그림자·그라디언트 금지, 전부 테마 토큰(`--canvas`·`--surface`·`--ink*`·`--hairline*`·`--accent*`), 라운드 8px(`--radius-card`/`--radius-control`), 간격 8px 단위(`--space-unit`). 뉴스는 데이터 잉크 아님.
- 테스트: **jsdom·@testing-library 도입 금지** — `react-dom/server`의 `renderToStaticMarkup` 정적 렌더 전략(IndicatorTable.test.tsx 주석에 명문화)만 사용.
- TS/React: 컴포넌트 PascalCase, 유틸 camelCase. 계산 로직은 컴포넌트가 아니라 `lib/` 순수 함수 + 단위 테스트.
- Python: 파일 I/O·print `encoding="utf-8"` 명시(이번엔 상수·docstring만 변경).
- 커밋: `feat:`/`docs:` prefix + 아래 푸터. 코드 변경 시 스펙 `tasks.md`(✅)·`implemented.md` 동기화.
  ```
  Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_01ADjYLr6iKTu1sm1MpEDPvh
  ```
- 명령 실행 위치: web 명령은 `C:\Users\jsmoon\Documents\todayfin\web`, pytest는 `C:\Users\jsmoon\Documents\todayfin\pipeline`.

---

### Task 1: 파이프라인 수집 상한 5→70

**Files:**
- Modify: `pipeline/collect_news.py:1` (docstring), `pipeline/collect_news.py:31` (`_LIMIT`)

**Interfaces:**
- Produces: `data/news.json`의 items가 최대 70건(스키마 불변 — 이후 태스크는 스키마만 의존, 건수 무의존)

- [ ] **Step 1: 상수·docstring 변경**

`pipeline/collect_news.py` 1행 docstring의 "상위 5건을" → "상위 70건을", 31행 `_LIMIT = 5` → `_LIMIT = 70`. 그 외 변경 금지(`_validate` 하한 1건 유지 — 피드가 70건 미만이어도 실패 아님).

- [ ] **Step 2: 기존 pytest 전체 통과 확인**

Run: `python -m pytest -q` (pipeline/ 에서)
Expected: 전부 PASS (기존 테스트는 `_LIMIT`을 참조하지 않음 — grep 확인 완료)

- [ ] **Step 3: Commit**

```bash
git add pipeline/collect_news.py
git commit -m "feat: 뉴스 수집 상한 5→70건 (news-page T1)"
```

---

### Task 2: lib/news.ts — stale 판정 공용화 + 정렬 순수 함수

**Files:**
- Create: `web/src/lib/news.ts`, `web/src/lib/news.test.ts`
- Modify: `web/src/components/Home.tsx` (isFreshNews·NEWS_MAX_AGE_MS 제거 → import)

**Interfaces:**
- Consumes: `types.ts`의 `NewsFeed`·`NewsItem`(기존)
- Produces:
  - `NEWS_MAX_AGE_MS: number`
  - `isFreshNews(feed: NewsFeed | null, now?: number): feed is NewsFeed` — now는 테스트 주입용 epoch ms, 기본 `Date.now()`
  - `sortByPublishedDesc(items: NewsItem[]): NewsItem[]` — 새 배열 반환, 안정 정렬, 파싱 불능 시각은 맨 뒤

- [ ] **Step 1: 실패하는 테스트 작성** — `web/src/lib/news.test.ts`

```ts
import { describe, expect, it } from 'vitest'
import { isFreshNews, NEWS_MAX_AGE_MS, sortByPublishedDesc } from './news'
import type { NewsFeed, NewsItem } from '../types'

const NOW = new Date('2026-08-08T12:00:00+00:00').getTime()

function feedAt(generatedAt: string, itemCount = 1): NewsFeed {
  return {
    generated_at: generatedAt,
    items: Array.from({ length: itemCount }, (_, i) => item(i)),
  }
}

function item(n: number, publishedAt = '2026-08-08T02:00:00+00:00'): NewsItem {
  return { title: `기사 ${n}`, url: `https://example.com/${n}`, source: `언론사${n}`, published_at: publishedAt }
}

describe('isFreshNews', () => {
  it('null·빈 items는 false', () => {
    expect(isFreshNews(null, NOW)).toBe(false)
    expect(isFreshNews(feedAt('2026-08-08T11:00:00+00:00', 0), NOW)).toBe(false)
  })

  it('24시간 이내는 true, 정확히 24시간 경계 포함', () => {
    expect(isFreshNews(feedAt('2026-08-08T11:00:00+00:00'), NOW)).toBe(true)
    expect(isFreshNews(feedAt(new Date(NOW - NEWS_MAX_AGE_MS).toISOString()), NOW)).toBe(true)
  })

  it('24시간 초과·generated_at 파싱 불능은 false', () => {
    expect(isFreshNews(feedAt(new Date(NOW - NEWS_MAX_AGE_MS - 1).toISOString()), NOW)).toBe(false)
    expect(isFreshNews(feedAt('not-a-date'), NOW)).toBe(false)
  })
})

describe('sortByPublishedDesc', () => {
  it('발행 시각 내림차순으로 정렬한 새 배열을 반환하고 원본은 불변', () => {
    const older = item(1, '2026-08-08T01:00:00+00:00')
    const newer = item(2, '2026-08-08T03:00:00+00:00')
    const input = [older, newer]
    const sorted = sortByPublishedDesc(input)
    expect(sorted.map((i) => i.title)).toEqual(['기사 2', '기사 1'])
    expect(input.map((i) => i.title)).toEqual(['기사 1', '기사 2'])
  })

  it('동일 시각은 원래(랭킹) 순서 유지 — 안정 정렬', () => {
    const a = item(1, '2026-08-08T02:00:00+00:00')
    const b = item(2, '2026-08-08T02:00:00+00:00')
    expect(sortByPublishedDesc([a, b]).map((i) => i.title)).toEqual(['기사 1', '기사 2'])
  })

  it('파싱 불능 시각은 맨 뒤로 보낸다', () => {
    const bad = item(1, 'not-a-date')
    const good = item(2, '2026-08-08T02:00:00+00:00')
    expect(sortByPublishedDesc([bad, good]).map((i) => i.title)).toEqual(['기사 2', '기사 1'])
  })
})
```

- [ ] **Step 2: 실패 확인**

Run: `npx vitest run src/lib/news.test.ts` (web/ 에서)
Expected: FAIL — "Cannot find module './news'"

- [ ] **Step 3: 구현** — `web/src/lib/news.ts`

```ts
import type { NewsFeed, NewsItem } from '../types'

/** generated_at이 이보다 오래되면 stale — 홈은 블록 숨김, 뉴스 페이지는 안내 문구
 * (docs/specs/news-page/requirements.md R5. 원래 Home.tsx 내부에 있던 것을 공용화) */
export const NEWS_MAX_AGE_MS = 24 * 60 * 60 * 1000

export function isFreshNews(feed: NewsFeed | null, now: number = Date.now()): feed is NewsFeed {
  if (!feed || !feed.items || feed.items.length === 0) return false
  const generated = new Date(feed.generated_at).getTime()
  return !Number.isNaN(generated) && now - generated <= NEWS_MAX_AGE_MS
}

/** 최신뉴스 탭 정렬. sort는 ES2019+ 안정 정렬 — 동일 시각은 원래(랭킹) 순서 유지 */
export function sortByPublishedDesc(items: NewsItem[]): NewsItem[] {
  const time = (item: NewsItem) => {
    const t = new Date(item.published_at).getTime()
    return Number.isNaN(t) ? -Infinity : t // 파싱 불능은 맨 뒤
  }
  return [...items].sort((a, b) => time(b) - time(a))
}
```

- [ ] **Step 4: 통과 확인**

Run: `npx vitest run src/lib/news.test.ts`
Expected: PASS (테스트 7건)

- [ ] **Step 5: Home.tsx를 import로 전환**

`web/src/components/Home.tsx`에서 지역 `NEWS_MAX_AGE_MS` 상수(18~20행)와 `isFreshNews` 함수(22~26행)를 **삭제**하고 import 추가:

```ts
import { isFreshNews } from '../lib/news'
```

동작 불변 — `isFreshNews(news)` 호출부는 그대로(now 기본값 사용).

- [ ] **Step 6: 전체 확인 후 Commit**

Run: `npx tsc -b && npx vitest run`
Expected: 컴파일 에러 없음, 전체 PASS

```bash
git add src/lib/news.ts src/lib/news.test.ts src/components/Home.tsx
git commit -m "feat: 뉴스 stale 판정·정렬을 lib/news 순수 함수로 (news-page T2)"
```

---

### Task 3: formatNewsRelativeTime — 토스식 상대 시각

**Files:**
- Modify: `web/src/lib/format.ts` (함수 추가), `web/src/lib/format.test.ts` (케이스 추가)

**Interfaces:**
- Consumes: 같은 파일의 `formatNewsTime(iso, now?)` (기존 — 7일 이상 위임)
- Produces: `formatNewsRelativeTime(iso: string, now?: Date): string`

- [ ] **Step 1: 실패하는 테스트 작성** — `web/src/lib/format.test.ts`에 describe 추가

```ts
import { formatNewsRelativeTime } from './format'  // 기존 import 라인에 병합

describe('formatNewsRelativeTime', () => {
  const now = new Date('2026-08-08T12:00:00+00:00')
  const at = (iso: string) => formatNewsRelativeTime(iso, now)

  it('60초 미만·미래 시각은 "방금 전"', () => {
    expect(at('2026-08-08T11:59:30+00:00')).toBe('방금 전')
    expect(at('2026-08-08T12:05:00+00:00')).toBe('방금 전') // 시계 오차 가드
  })

  it('분 단위 — 59분까지', () => {
    expect(at('2026-08-08T11:59:00+00:00')).toBe('1분 전')
    expect(at('2026-08-08T11:01:00+00:00')).toBe('59분 전')
  })

  it('시간 단위 — 60분부터 23시간까지', () => {
    expect(at('2026-08-08T11:00:00+00:00')).toBe('1시간 전')
    expect(at('2026-08-07T12:00:01+00:00')).toBe('23시간 전')
  })

  it('일 단위 — 24시간부터 6일까지', () => {
    expect(at('2026-08-07T12:00:00+00:00')).toBe('1일 전')
    expect(at('2026-08-01T12:00:01+00:00')).toBe('6일 전')
  })

  it('7일 이상은 formatNewsTime(KST 절대 시각)으로 위임', () => {
    expect(at('2026-08-01T12:00:00+00:00')).toBe('08.01 21:00') // KST = UTC+9
  })

  it('파싱 불능은 빈 문자열', () => {
    expect(at('not-a-date')).toBe('')
  })
})
```

- [ ] **Step 2: 실패 확인**

Run: `npx vitest run src/lib/format.test.ts`
Expected: FAIL — formatNewsRelativeTime not exported

- [ ] **Step 3: 구현** — `web/src/lib/format.ts` 끝에 추가

```ts
/**
 * 뉴스 피드 행의 상대 시각(토스식). 헤드라인 블록의 KST 절대 시각(formatNewsTime)과 달리
 * 뷰어 시계 기준 diff. 미래 시각(수집·클라 시계 오차)은 "방금 전" 가드, 7일 이상은
 * 절대 시각으로 위임. now는 테스트 주입용.
 */
export function formatNewsRelativeTime(iso: string, now: Date = new Date()): string {
  const date = new Date(iso);
  if (Number.isNaN(date.getTime())) return '';
  const diffMs = now.getTime() - date.getTime();
  if (diffMs < 60 * 1000) return '방금 전'; // 미래 시각 포함
  const minutes = Math.floor(diffMs / (60 * 1000));
  if (minutes < 60) return `${minutes}분 전`;
  const hours = Math.floor(diffMs / (60 * 60 * 1000));
  if (hours < 24) return `${hours}시간 전`;
  const days = Math.floor(diffMs / (24 * 60 * 60 * 1000));
  if (days < 7) return `${days}일 전`;
  return formatNewsTime(iso, now);
}
```

- [ ] **Step 4: 통과 확인 후 Commit**

Run: `npx vitest run src/lib/format.test.ts`
Expected: PASS

```bash
git add src/lib/format.ts src/lib/format.test.ts
git commit -m "feat: formatNewsRelativeTime 상대 시각 포맷 (news-page T3)"
```

---

### Task 4: `/news` 라우트 + GNB 뉴스 탭

**Files:**
- Modify: `web/src/lib/route.ts`, `web/src/lib/route.test.ts`, `web/src/components/Gnb.tsx`

**Interfaces:**
- Produces: `Route` union에 `{ name: "news" }`, `GNB_TABS` 2번째 항목 `{ label: '뉴스', hash: '#/news', routeName: 'news' }`

- [ ] **Step 1: 실패하는 테스트 작성** — `web/src/lib/route.test.ts`에 추가

```ts
it("#/news 는 뉴스 페이지", () => {
  expect(parseHash("#/news")).toEqual({ name: "news" });
});

it("#/news 하위 변형 경로는 홈으로 폴백", () => {
  expect(parseHash("#/news/x")).toEqual({ name: "home" });
});
```

- [ ] **Step 2: 실패 확인**

Run: `npx vitest run src/lib/route.test.ts`
Expected: FAIL — `{ name: "home" }` 반환

- [ ] **Step 3: 구현**

`web/src/lib/route.ts`의 `Route` union에 `| { name: "news" }` 추가, `if (clean === "/about")` 라인 옆에:

```ts
if (clean === "/news") return { name: "news" };
```

`web/src/components/Gnb.tsx`의 `GNB_TABS`를:

```ts
export const GNB_TABS: GnbTab[] = [
  { label: '홈', hash: '#/', routeName: 'home' },
  { label: '뉴스', hash: '#/news', routeName: 'news' },
  { label: '소개', hash: '#/about', routeName: 'about' },
  { label: '알상무', hash: '#/alsangmoo', routeName: 'alsangmoo' },
]
```

- [ ] **Step 4: 통과 확인 후 Commit**

Run: `npx tsc -b && npx vitest run src/lib/route.test.ts`
Expected: PASS (App.tsx는 아직 news 라우트를 안 그리므로 컴파일만 확인 — parseHash가 news를 반환하면 App은 홈도 뉴스도 아닌 빈 main을 그리는 중간 상태. Task 7에서 해소)

```bash
git add src/lib/route.ts src/lib/route.test.ts src/components/Gnb.tsx
git commit -m "feat: #/news 라우트·GNB 뉴스 탭 (news-page T4)"
```

---

### Task 5: NewsHeadlines에 className·moreHref prop + 홈 더보기

**Files:**
- Modify: `web/src/components/NewsHeadlines.tsx`, `web/src/components/NewsHeadlines.css`, `web/src/components/NewsHeadlines.test.tsx`, `web/src/components/Home.tsx`

**Interfaces:**
- Produces: `NewsHeadlines({ items, className?, moreHref? })` — className은 section 클래스에 병합, moreHref 전달 시 제목 행 우측 "더보기 →" 링크(홈에서만 전달 — 뉴스 페이지에서는 미전달로 자기 링크 방지)

- [ ] **Step 1: 실패하는 테스트 작성** — `NewsHeadlines.test.tsx`에 추가

```ts
it('moreHref 전달 시 제목 행에 더보기 링크를 그린다', () => {
  const html = renderToStaticMarkup(<NewsHeadlines items={[item(1)]} moreHref="#/news" />)
  expect(html).toContain('news-more')
  expect(html).toContain('href="#/news"')
})

it('moreHref 미전달 시 더보기 링크가 없다', () => {
  const html = renderToStaticMarkup(<NewsHeadlines items={[item(1)]} />)
  expect(html).not.toContain('news-more')
})

it('className을 section 클래스에 병합한다', () => {
  const html = renderToStaticMarkup(<NewsHeadlines items={[item(1)]} className="news-pinned" />)
  expect(html).toContain('class="news news-pinned"')
})
```

- [ ] **Step 2: 실패 확인**

Run: `npx vitest run src/components/NewsHeadlines.test.tsx`
Expected: FAIL — TS prop 에러 또는 news-more 부재

- [ ] **Step 3: 구현** — `NewsHeadlines.tsx`

```tsx
export interface NewsHeadlinesProps {
  items: NewsItem[]
  /** section 클래스에 병합 — 뉴스 페이지 고정 블록의 --surface 배경용(news-pinned) */
  className?: string
  /** 전달 시 제목 행 우측 "더보기 →" — 홈에서만 전달(뉴스 페이지 자기 링크 방지) */
  moreHref?: string
}

export default function NewsHeadlines({ items, className, moreHref }: NewsHeadlinesProps): JSX.Element {
  return (
    <section className={className ? `news ${className}` : 'news'} aria-label="주요 뉴스">
      <div className="news-title-row">
        <h2 className="news-title">주요 뉴스</h2>
        {moreHref && (
          <a className="news-more" href={moreHref}>
            더보기 →
          </a>
        )}
      </div>
      <ol className="news-list">{/* 기존 그대로 */}</ol>
    </section>
  )
}
```

(ol 내부는 기존 코드 그대로 유지 — 변경 금지)

`NewsHeadlines.css`: `.news-title`의 `margin: 0 0 var(--space-unit)`을 `margin: 0`으로 바꾸고 추가:

```css
.news-title-row {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  margin: 0 0 var(--space-unit);
}

.news-more {
  flex-shrink: 0;
  font-size: var(--fs-caption-sm);
  color: var(--ink-muted);
  text-decoration: none;
}

.news-more:hover,
.news-more:focus-visible {
  color: var(--accent);
  text-decoration: underline;
}
```

`Home.tsx` 렌더부: `<NewsHeadlines items={news.items.slice(0, 5)} />` → `<NewsHeadlines items={news.items.slice(0, 5)} moreHref="#/news" />`

- [ ] **Step 4: 통과 확인 후 Commit**

Run: `npx vitest run src/components/NewsHeadlines.test.tsx && npx tsc -b`
Expected: PASS(기존 3건 + 신규 3건)

```bash
git add src/components/NewsHeadlines.tsx src/components/NewsHeadlines.css src/components/NewsHeadlines.test.tsx src/components/Home.tsx
git commit -m "feat: 주요 뉴스 블록 더보기 링크·className prop (news-page T5)"
```

---

### Task 6: NewsView(프레젠테이션) + News(컨테이너) + News.css

**Files:**
- Create: `web/src/components/NewsView.tsx`, `web/src/components/News.tsx`, `web/src/components/News.css`, `web/src/components/NewsView.test.tsx`, `web/src/components/News.test.tsx`
- Modify: `web/src/lib/news.ts`·`web/src/lib/news.test.ts` (`newsPageState` 추가)

**Interfaces:**
- Consumes: `sortByPublishedDesc`·`isFreshNews`(T2), `formatNewsRelativeTime`(T3), `NewsHeadlines className prop`(T5), `usePolledJson`(기존)
- Produces: `News(): JSX.Element | null`(default export — Task 7의 App이 렌더), `NewsView({ items, initialTab? })`, `newsPageState(data: NewsFeed | null, error: boolean): 'loading' | 'unavailable' | 'ready'`

- [ ] **Step 1: newsPageState 실패 테스트** — `web/src/lib/news.test.ts`에 추가

```ts
import { newsPageState } from './news'  // 기존 import 라인에 병합

describe('newsPageState', () => {
  const fresh = feedAt('2026-08-08T11:00:00+00:00')
  const stale = feedAt('2026-08-06T11:00:00+00:00')

  it('첫 응답 전(data null, error false)은 loading — 오류 문구 플래시 방지', () => {
    expect(newsPageState(null, false, NOW)).toBe('loading')
  })

  it('한 번도 성공 못 함(error) 또는 24h stale은 unavailable', () => {
    expect(newsPageState(null, true, NOW)).toBe('unavailable')
    expect(newsPageState(stale, false, NOW)).toBe('unavailable')
  })

  it('fresh 데이터는 ready', () => {
    expect(newsPageState(fresh, false, NOW)).toBe('ready')
  })
})
```

- [ ] **Step 2: 실패 확인**

Run: `npx vitest run src/lib/news.test.ts`
Expected: FAIL — newsPageState not exported

- [ ] **Step 3: newsPageState 구현** — `web/src/lib/news.ts`에 추가

```ts
export type NewsPageState = 'loading' | 'unavailable' | 'ready'

/** 뉴스 페이지 3-상태 분기(R5) — usePolledJson의 {data, error}를 그대로 받는다.
 * 로딩을 실패로 오판하면 진입 때마다 오류 문구가 플래시된다(적대적 검증 반영). */
export function newsPageState(data: NewsFeed | null, error: boolean, now: number = Date.now()): NewsPageState {
  if (data === null && !error) return 'loading'
  if (error || !isFreshNews(data, now)) return 'unavailable'
  return 'ready'
}
```

Run: `npx vitest run src/lib/news.test.ts` → PASS

- [ ] **Step 4: NewsView 실패 테스트** — `web/src/components/NewsView.test.tsx`

```tsx
/**
 * NewsView 렌더 테스트 — docs/specs/news-page/design.md.
 * jsdom 없이 정적 마크업 검증(repo 전략) — 탭 전환 클릭 대신 initialTab 주입으로
 * 두 상태를 각각 렌더한다. fetch·상태 분기는 News(컨테이너) 소관.
 */
import { describe, expect, it } from 'vitest'
import { renderToStaticMarkup } from 'react-dom/server'
import NewsView from './NewsView'
import type { NewsItem } from '../types'

function item(n: number, publishedAt: string, overrides: Partial<NewsItem> = {}): NewsItem {
  return {
    title: `기사 ${n}`,
    url: `https://news.google.com/rss/articles/item-${n}`,
    source: `언론사${n}`,
    published_at: publishedAt,
    ...overrides,
  }
}

// 랭킹순 8건 — 발행 시각은 랭킹과 어긋나게(7번이 가장 최신) 구성해 정렬 검증
const items: NewsItem[] = [
  item(1, '2026-08-08T05:00:00+00:00'),
  item(2, '2026-08-08T04:00:00+00:00'),
  item(3, '2026-08-08T03:00:00+00:00'),
  item(4, '2026-08-08T02:00:00+00:00'),
  item(5, '2026-08-08T01:00:00+00:00'),
  item(6, '2026-08-08T00:00:00+00:00'),
  item(7, '2026-08-08T06:00:00+00:00'),
  item(8, '2026-08-07T23:00:00+00:00'),
]

describe('NewsView', () => {
  it('상단 고정 블록에 1~5위, 주요뉴스 탭(기본)에 6번째부터 랭킹순', () => {
    const html = renderToStaticMarkup(<NewsView items={items} />)
    expect(html.match(/news-item/g)).toHaveLength(5) // 고정 블록 5행
    expect(html.match(/news-feed-item/g)).toHaveLength(3) // 피드 6·7·8
    expect(html.indexOf('기사 6')).toBeLessThan(html.indexOf('기사 7'))
    expect(html).toContain('news-pinned') // 구분감 배경 클래스
  })

  it('initialTab=latest면 전체를 발행 시각 내림차순으로', () => {
    const html = renderToStaticMarkup(<NewsView items={items} initialTab="latest" />)
    expect(html.match(/news-feed-item/g)).toHaveLength(8)
    // 고정 블록(기사 1~5가 먼저 렌더됨)을 제외한 피드 영역에서만 순서 비교
    const feed = html.slice(html.indexOf('news-feed'))
    expect(feed.indexOf('기사 7')).toBeLessThan(feed.indexOf('기사 1')) // 7번이 최신
  })

  it('활성 탭만 aria-pressed=true', () => {
    const html = renderToStaticMarkup(<NewsView items={items} />)
    expect(html).toContain('aria-pressed="true"')
    expect(html).toContain('aria-pressed="false"')
    expect(html).toContain('news-tab-active')
  })

  it('피드 링크는 새 탭 + noopener·noreferrer', () => {
    const html = renderToStaticMarkup(<NewsView items={items} />)
    expect(html).toContain('target="_blank"')
    expect(html).toContain('rel="noopener noreferrer"')
  })

  it('source가 null이면 구분점 없이 상대 시각만', () => {
    // 피드에는 null source 1건만 — 구분점 '·'은 피드 메타에서만 쓰이므로(고정 블록은
    // 별도 span 문법) 문서 전체에 '·'가 없어야 한다
    const nullSource = [item(6, '2026-08-08T05:00:00+00:00', { source: null })]
    const html = renderToStaticMarkup(
      <NewsView items={[...items.slice(0, 5), ...nullSource]} />,
    )
    expect(html).not.toContain('·') // 고아 구분점 없음
  })

  it('items 5건 이하면 주요뉴스 탭은 빈 문구', () => {
    const html = renderToStaticMarkup(<NewsView items={items.slice(0, 5)} />)
    expect(html).toContain('더 표시할 뉴스가 없습니다')
    expect(html).not.toContain('news-feed-item')
  })
})
```

- [ ] **Step 5: 실패 확인**

Run: `npx vitest run src/components/NewsView.test.tsx`
Expected: FAIL — "Cannot find module './NewsView'"

- [ ] **Step 6: NewsView 구현** — `web/src/components/NewsView.tsx`

```tsx
import { useState } from 'react'
import type { JSX } from 'react'
import type { NewsItem } from '../types'
import NewsHeadlines from './NewsHeadlines'
import { sortByPublishedDesc } from '../lib/news'
import { formatNewsRelativeTime } from '../lib/format'
import './News.css'

export type NewsTab = 'major' | 'latest'

const TABS: { key: NewsTab; label: string }[] = [
  { key: 'major', label: '주요뉴스' },
  { key: 'latest', label: '최신뉴스' },
]

/**
 * 뉴스 페이지 프레젠테이션 — docs/specs/news-page/design.md.
 * 주요뉴스 = 랭킹순(고정 5건 제외 6번째부터), 최신뉴스 = 전체 발행 시각 내림차순.
 * initialTab은 테스트 주입용(정적 렌더 전략 — 클릭 시뮬레이션 없이 두 상태를 각각 검증).
 */
export default function NewsView({
  items,
  initialTab = 'major',
}: {
  items: NewsItem[]
  initialTab?: NewsTab
}): JSX.Element {
  const [tab, setTab] = useState<NewsTab>(initialTab)
  const feedItems = tab === 'major' ? items.slice(5) : sortByPublishedDesc(items)

  return (
    <div className="news-page">
      <h1 className="sr-only">뉴스</h1>
      <NewsHeadlines items={items.slice(0, 5)} className="news-pinned" />

      <div className="news-tabs">
        {TABS.map(({ key, label }) => (
          <button
            key={key}
            type="button"
            aria-pressed={tab === key}
            className={tab === key ? 'news-tab news-tab-active' : 'news-tab'}
            onClick={() => setTab(key)}
          >
            {label}
          </button>
        ))}
      </div>

      {feedItems.length === 0 ? (
        <p className="news-feed-empty muted">더 표시할 뉴스가 없습니다</p>
      ) : (
        <ol className="news-feed">
          {feedItems.map((item) => {
            const time = formatNewsRelativeTime(item.published_at)
            return (
              <li key={item.url} className="news-feed-item">
                <a className="news-feed-link" href={item.url} target="_blank" rel="noopener noreferrer">
                  <span className="news-feed-meta">
                    {item.source}
                    {item.source && time ? ' · ' : ''}
                    {time}
                  </span>
                  <span className="news-feed-title">{item.title}</span>
                </a>
              </li>
            )
          })}
        </ol>
      )}
    </div>
  )
}
```

- [ ] **Step 7: News 컨테이너 실패 테스트** — `web/src/components/News.test.tsx`

```tsx
/**
 * News 컨테이너 — 정적 렌더에서는 useEffect(fetch)가 돌지 않아 항상 로딩 상태.
 * 로딩 = 빈 본문(오류 문구 플래시 방지, R5)을 그대로 검증한다.
 * unavailable/ready 분기는 newsPageState 단위 테스트(lib/news.test.ts)가 커버.
 */
import { describe, expect, it } from 'vitest'
import { renderToStaticMarkup } from 'react-dom/server'
import News from './News'

describe('News', () => {
  it('로딩 중(첫 응답 전)에는 아무것도 렌더하지 않는다', () => {
    const html = renderToStaticMarkup(<News />)
    expect(html).toBe('')
  })
})
```

- [ ] **Step 8: 실패 확인**

Run: `npx vitest run src/components/News.test.tsx`
Expected: FAIL — "Cannot find module './News'"

- [ ] **Step 9: News 구현** — `web/src/components/News.tsx`

```tsx
import type { JSX } from 'react'
import type { NewsFeed } from '../types'
import NewsView from './NewsView'
import { newsPageState } from '../lib/news'
import { usePolledJson } from '../lib/usePolledJson'
import './News.css'

/** 뉴스 폴링 주기 — 수집이 매시간이라 홈과 같은 5분이면 충분 */
const NEWS_POLL_INTERVAL_MS = 5 * 60 * 1000

/**
 * 뉴스 페이지 컨테이너 — fetch와 3-상태 분기(docs/specs/news-page R5)만 담당.
 * 첫 성공 이후 일시 실패는 usePolledJson이 기존 값을 유지하므로 unavailable로 떨어지지 않는다.
 */
export default function News(): JSX.Element | null {
  const { data, error } = usePolledJson<NewsFeed>(
    `${import.meta.env.BASE_URL}data/news.json`,
    NEWS_POLL_INTERVAL_MS,
  )

  const state = newsPageState(data, error)
  if (state === 'loading') return null

  if (state === 'unavailable') {
    return (
      <div className="news-page">
        <div className="news-page-error">
          <p className="news-page-error-message">뉴스를 불러오지 못했습니다.</p>
          <p className="news-page-error-hint muted">잠시 후 새로고침해 다시 시도해 주세요.</p>
        </div>
      </div>
    )
  }

  // state === 'ready' — newsPageState가 isFreshNews를 통과시켰으므로 data는 non-null
  return <NewsView items={(data as NewsFeed).items} />
}
```

- [ ] **Step 10: News.css 작성** — `web/src/components/News.css`

```css
/* 뉴스 페이지 — docs/specs/news-page/design.md.
   전부 테마 토큰(다크모드 자동), 그림자·그라디언트 금지. 데이터 잉크 아님. */

/* Home과 동일한 컨테이너 문법 — app-main은 폭을 주지 않는다 */
.news-page {
  max-width: var(--max-width-dashboard);
  margin: 0 auto;
  padding: calc(var(--space-unit) * 6) var(--pad-card-mobile) calc(var(--space-unit) * 8);
}

@media (min-width: 850px) {
  .news-page {
    padding-left: var(--pad-card);
    padding-right: var(--pad-card);
  }
}

/* 상단 고정 헤드라인 — 홈과 동일 문법에 배경만 달리해 구분감(R2) */
.news-pinned {
  background: var(--surface);
}

/* 탭 — Detail의 pill-btn과 동일 토큰 조합을 복제 선언(모달 전용 CSS import 금지,
   공용화는 세 번째 사용처가 생길 때) */
.news-tabs {
  display: flex;
  gap: var(--space-unit);
  margin: 0 0 calc(var(--space-unit) * 2);
}

.news-tab {
  border-radius: var(--radius-control);
  border: 1px solid var(--hairline);
  background: var(--canvas);
  color: var(--ink);
  padding: 8px 20px;
  font-size: var(--fs-body-sm);
  cursor: pointer;
  line-height: 1.2;
}

.news-tab:hover {
  border-color: var(--accent);
}

.news-tab-active {
  background: var(--accent);
  border-color: var(--accent);
  color: var(--on-accent);
}

/* 피드 — 카드 박스 없이 개방형 리스트(고정 블록과 위계 대비) */
.news-feed {
  list-style: none;
  margin: 0;
  padding: 0;
}

.news-feed-item {
  border-bottom: 1px solid var(--hairline-soft);
}

.news-feed-item:last-child {
  border-bottom: none;
}

.news-feed-link {
  display: block;
  padding: calc(var(--space-unit) * 1.5) 0;
  text-decoration: none;
}

.news-feed-meta {
  display: block;
  font-size: var(--fs-caption-sm);
  color: var(--ink-muted);
  margin-bottom: calc(var(--space-unit) / 2);
}

/* 2줄 클램프 — -webkit-line-clamp는 전 브라우저 지원(사실상 표준) */
.news-feed-title {
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
  font-size: var(--fs-body-md);
  color: var(--ink);
  line-height: 1.5;
}

.news-feed-link:hover .news-feed-title,
.news-feed-link:focus-visible .news-feed-title {
  color: var(--accent);
  text-decoration: underline;
}

.news-feed-empty {
  font-size: var(--fs-body-sm);
  padding: calc(var(--space-unit) * 3) 0;
}

.news-page-error {
  padding: calc(var(--space-unit) * 6) 0;
  text-align: center;
}

.news-page-error-message {
  font-size: var(--fs-heading-sm);
  color: var(--ink);
  margin: 0 0 var(--space-unit);
}

.news-page-error-hint {
  font-size: var(--fs-body-sm);
  margin: 0;
}

/* 모바일 — 행 패딩 축소(헤드라인 블록 출처 숨김은 NewsHeadlines.css 기존 규칙 상속) */
@media (max-width: 640px) {
  .news-feed-link {
    padding: var(--space-unit) 0;
  }
}
```

- [ ] **Step 11: 통과 확인 후 Commit**

Run: `npx tsc -b && npx vitest run`
Expected: 전체 PASS (NewsView 6건·News 1건·newsPageState 3건 신규)

```bash
git add src/components/NewsView.tsx src/components/News.tsx src/components/News.css src/components/NewsView.test.tsx src/components/News.test.tsx src/lib/news.ts src/lib/news.test.ts
git commit -m "feat: 뉴스 페이지 컴포넌트 — 고정 헤드라인·주요/최신 탭 피드 (news-page T6)"
```

---

### Task 7: App 배선 — news 라우트 렌더 + 티커 노출

**Files:**
- Modify: `web/src/App.tsx`

**Interfaces:**
- Consumes: `News` default export(T6), `Route` news variant(T4)

- [ ] **Step 1: App.tsx 수정**

import 추가(다른 페이지 컴포넌트 import 옆):

```ts
import News from './components/News'
```

`showTicker`(44행 부근) 변경:

```ts
const showTicker = route.name === 'home' || route.name === 'detail' || route.name === 'news'
```

`<main className="app-main">` 안, about 블록 앞이나 뒤에 추가:

```tsx
{route.name === 'news' && (
  <ErrorBoundary key="news">
    <News />
  </ErrorBoundary>
)}
```

(티커 클릭 시 홈+상세 모달로 이탈하고 닫으면 홈에 남는 동작은 스펙에서 수용 확정 — 코드 변경 없음)

- [ ] **Step 2: 전체 확인 후 Commit**

Run: `npx tsc -b && npx vitest run && npm run lint`
Expected: 전부 PASS

```bash
git add src/App.tsx
git commit -m "feat: 뉴스 페이지 라우트 배선·티커 노출 (news-page T7)"
```

---

### Task 8: 문서 동기화 (스펙 R8 + 접점 6곳)

**Files:**
- Modify: `docs/data-rights.md`, `docs/steering/design.md`, `docs/steering/structure.md`, `docs/steering/tech.md`, `web/src/components/About.tsx`, `docs/specs/news-headlines/requirements.md`

**Interfaces:** 없음(문서만)

- [ ] **Step 1: data-rights.md** — Google News 행(26행)의 완화 문구 중 `5건 제한` 부분을 갱신:

기존: `헤드라인·출처명·발행시각·원문 링크만 표시(본문·이미지 미수록, 5건 제한)`
변경: `헤드라인·출처명·발행시각·원문 링크만 표시(본문·이미지 미수록, 피드 제공 건수 내 ~70건 — 홈 5건 + 뉴스 페이지 전체)`

- [ ] **Step 2: steering design.md 레이아웃 절**

- "주요 뉴스 헤드라인" 항목 끝에 추가: `2026-08-08 news-page: 수집 상한 70건으로 확대, #/news 뉴스 페이지 신설(상단 고정 5건 + 주요뉴스(랭킹순, 6번째부터)/최신뉴스(시간순) 탭 — docs/specs/news-page/).`
- GNB 항목: `페이지 탭 3개(홈/소개/알상무` → `페이지 탭 4개(홈/뉴스/소개/알상무`
- 티커 바 항목: `홈(#/)·상세(#/i/{id})에서만 노출하고 소개·알상무 페이지에서는 숨기며` → `홈(#/)·뉴스(#/news)·상세(#/i/{id})에서만 노출하고 소개·알상무 페이지에서는 숨기며`

- [ ] **Step 3: steering structure.md** — 데이터 계약 절 news.json 행:

기존: `Google News 경제 헤드라인 상위 5건 (media-collect, 매시간·실패 시 기존 유지)`
변경: `Google News 경제 헤드라인 상위 70건 — 홈은 5건 표시, 뉴스 페이지는 전체 (media-collect, 매시간·실패 시 기존 유지)`

- [ ] **Step 4: steering tech.md** — 소스 표 37행:

기존: `| 경제뉴스 헤드라인 5건 |`
변경: `| 경제뉴스 헤드라인 70건(홈 5건 표시) |`

- [ ] **Step 5: About.tsx** — 147~148행 문장 갱신:

기존: `홈 상단의 주요 뉴스는 Google News가 국내 언론사 경제 기사에서 추린 헤드라인을 매시간 가져와 표시하며, 제목을 클릭하면 해당 언론사 원문으로 이동합니다.`
변경: `홈 상단의 주요 뉴스 5건과 뉴스 페이지의 전체 피드(약 70건)는 Google News가 국내 언론사 경제 기사에서 추린 헤드라인을 매시간 가져와 표시하며, 제목을 클릭하면 해당 언론사 원문으로 이동합니다.`

- [ ] **Step 6: news-headlines 스펙 supersession** — `docs/specs/news-headlines/requirements.md` 상단(3행 인용 블록 아래)에 추가:

```
> 2026-08-08 개정: 수집 상한은 news-page 스펙(docs/specs/news-page/)으로 5→70건 확대.
> 홈 표시는 여전히 5건 — 이 문서의 "5건" 서술은 홈 표시 기준으로만 유효.
```

- [ ] **Step 7: index.html 확인(변경 없음 기록)** — 현행 description "…핵심 지표와 주요 경제뉴스를 한눈에"는 뉴스 페이지 신설 후에도 정확 — 변경하지 않는다(implemented.md에 판단 기록).

- [ ] **Step 8: Commit**

```bash
git add docs/data-rights.md docs/steering/design.md docs/steering/structure.md docs/steering/tech.md web/src/components/About.tsx docs/specs/news-headlines/requirements.md
git commit -m "docs: 뉴스 70건 확대·뉴스 페이지 접점 문서 동기화 (news-page T8)"
```

---

### Task 9: 전체 검증 게이트

**Files:** 없음(검증만)

- [ ] **Step 1: web 전체** — web/ 에서:

Run: `npx tsc -b && npx vitest run && npm run lint && npm run build`
Expected: 컴파일·테스트·린트·빌드 전부 성공

- [ ] **Step 2: pipeline 전체** — pipeline/ 에서:

Run: `python -m pytest -q`
Expected: 전부 PASS

- [ ] **Step 3: 스펙 tasks.md 동기화 커밋**

`docs/specs/news-page/tasks.md`의 T1~T9를 ✅로, `implemented.md` "계획과의 편차" 절 갱신(편차 없으면 "없음" 기록).

```bash
git add docs/specs/news-page/tasks.md docs/specs/news-page/implemented.md
git commit -m "docs: news-page T1~T9 완료 동기화"
```

---

### Task 10: 로컬 실측 (수집 스모크 + 브라우저)

**Files:** 없음(검증만 — `web/public/data/news.json`은 gitignore된 dev 사본)

- [ ] **Step 1: 실수집 스모크** — repo 루트에서:

Run: `python pipeline/collect_news.py --data-dir web/public/data`
Expected: `[ok] 헤드라인 70건 저장 (최신: …)` — 70건 미만이어도 60건대면 정상(피드 제공량). 한글 제목·출처 정상 확인.

- [ ] **Step 2: dev 서버 + 브라우저 실측** — web/ 에서 `npm run dev` 후 claude-in-chrome으로:

- `http://localhost:5173/#/news` 접속: 상단 고정 5건(--surface 배경 구분감) + 주요뉴스 탭 6번째부터
- 최신뉴스 탭 클릭: 전체 시간순 재정렬, 상대 시각("n시간 전") 표시
- 다크모드 토글: 위화감 없는지
- 모바일 뷰포트(≤640px): 행 레이아웃·고정 블록 출처 숨김
- 홈 `#/`: 주요 뉴스 블록 우측 "더보기 →" → 클릭 시 뉴스 페이지 이동
- GNB: 뉴스 탭 활성 표시, 티커 바 하단 노출
- 피드 행 클릭: 새 탭에서 구글 경유 원문 도달

Expected: 전 항목 정상. 문제 발견 시 수정 후 Task 9 재실행.

---

### Task 11: 배포 + 실측

**Files:** 없음(운영)

- [ ] **Step 1: push** — 로컬 커밋 전체 push:

Run: `git push origin main`

- [ ] **Step 2: 즉시 media-collect dispatch** (주요뉴스 탭 빈 화면 윈도 최소화 — 스펙 수용 조건):

Run: `gh workflow run media-collect.yml`, 몇 초 대기 후
`gh run watch $(gh run list --workflow=media-collect.yml --limit 1 --json databaseId -q '.[0].databaseId')`
(dispatch 직후엔 run이 목록에 없을 수 있음 — 목록이 비면 잠시 후 재시도)
Expected: 성공 — data/news.json 70건 커밋 → Pages 재배포

- [ ] **Step 3: 배포 사이트 실측** — 배포 완료 후 실제 URL에서 Task 10 Step 2와 동일 항목 + 70건 표시 확인.

- [ ] **Step 4: 완료 동기화**

`docs/specs/news-page/tasks.md` T10·T11 ✅, `implemented.md` 검증 절에 실측 결과 기록.

```bash
git add docs/specs/news-page/tasks.md docs/specs/news-page/implemented.md
git commit -m "docs: news-page 배포 실측 완료 동기화" && git push origin main
```
